{{- if .Values.codexToml }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "codex-gitlab-review.fullname" . }}-codex-config
  labels:
    app.kubernetes.io/name: {{ include "codex-gitlab-review.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  config.toml: |
{{ .Values.codexToml | nindent 4 }}
{{- end }}
